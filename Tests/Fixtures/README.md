# Test Fixtures

Suggested real-world fixture sets for macOS validation:
- perfect metadata pair
- timestamp-only pair
- ambiguous pair candidates
- unmatched image
- unmatched MOV
- already-normalized pair
- collision case

These are documented placeholders for now because real Apple media samples are not bundled in the repository yet.
